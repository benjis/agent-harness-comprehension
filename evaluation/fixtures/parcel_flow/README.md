# ParcelFlow

ParcelFlow is a small Ruby library for dispatching shipments from local stock. It has no runtime dependencies outside Ruby's standard library.

Run the visible test suite with:

```sh
ruby -Ilib -Itest test/all_test.rb
```
