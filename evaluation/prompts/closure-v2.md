# Instrument v2 bounded closure

The implementation has settled. Do not modify source, tests, or task files during this step.

Inspect `TASK.md`, the final diff, final source, visible tests, and the existing comprehension ledger. Ensure the current ledger has one supported checkpoint for each of:

- the current goal;
- the final implementation decision;
- one current invariant or constraint;
- final validation.

Reuse an existing eligible checkpoint when it already covers the item. Record a missing checkpoint only when its summary and evidence are supported by the frozen final state. Do not invent or reconstruct a failure, revision, rejected alternative, causal link, or supersession relationship that is absent from the observable session history. Do not include private chain-of-thought, raw prompts, secrets, or raw tool output.

If the required current coverage is already complete, make no semantic writes. Report completion without changing repository files.
