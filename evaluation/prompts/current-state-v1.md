# Current-state artifact prompt v1

Using only the frozen repository, `TASK.md`, `diff.patch`, and visible-test output, produce a JSON document that conforms to `evaluation/schemas/current-state-v1.example.json`.

Describe the final implementation, not the implementation journey. Cover every required section, use stable `cur-NNN` claim IDs, and attach at least one precise evidence locator to every claim. Do not infer runtime history or private reasoning.
